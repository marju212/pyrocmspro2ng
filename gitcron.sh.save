
#!/bin/bash
# Proper header for a Bash script.


now="$(date +'%m/%d/%Y %H:%M:%S')"

DIR=$(readlink -f ${0%/*})




cd $dir && git status && git pull && git add --all && git commit -am "$now"  && git status
